query ServerPulls {
    pullRequests {
        edges {
            cursor
            node {
                owner
                repo
                number
                title
                assignees
            }
        }
    }
}
